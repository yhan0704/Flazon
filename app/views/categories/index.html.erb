<%= render partial: 'shared/header_form'%>
<%= render partial: 'shared/nav_form' %>
<h1>Our Store Categories</h1>
<% @categories.each do |category| %>
  <ul>
      <li><%= link_to category.name, category_path(category) %> (Count Item(s)): <%= category.items.count %> Edit Delete</li>
  </ul>
<% end %>
<%= link_to "New Category", new_category_path %>
<%= render partial: 'shared/footer_form' %>
