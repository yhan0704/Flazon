<h1>Our Store Categories</h1>

<% @categories.each do |category| %>
<ul>
    <li><%= link_to category.name, category_path(category) %></li>
    Edit Delete<br><br>
</ul>
<% end %>

<%= link_to "New Category", new_category_path %>
