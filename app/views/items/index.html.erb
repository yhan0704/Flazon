<%= render partial: 'shared/header_form' %>
<%= render partial: 'shared/nav_form' %>
<div class=""> <!-- Main div  -->
      <div class=""> <!-- Items div  -->
        <h1>Items</h1>
        <% @items.each do |item| %>
        <ul>
          <li><%= link_to item.name, item_path(item)  %></li>
        </ul>
        <% end %>
      </div> <!-- Items div closing  -->

      <div class=""> <!-- Action Buttons div  -->
        <%= link_to "Create New Item", new_item_path %>
      </div> <!-- Action Buttons div closing -->



    </div> <!-- Main div closing  -->
<%= render partial: 'shared/footer_form' %>















